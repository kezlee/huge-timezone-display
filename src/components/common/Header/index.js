import React, { useState } from 'react';
import { HeaderContainer } from './styles';

const Header = () => {
    return (
        <HeaderContainer>
            test
        </HeaderContainer>
    );
};

export default Header;